{
  "name": "bananastudioai-hostglow",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "next build",
    "start": "next start -p 3000",
    "e2e": "playwright test",
    "ci": "npm run build && (npm run start &) && npx wait-on http://localhost:3000 && npm run e2e"
  },
  "dependencies": {
    "next": "15.0.0",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.9.0",
    "@playwright/test": "^1.48.0",
    "wait-on": "^7.2.0"
  }
}
