{
  "name": "🍌-hostglow-by-banana-studio-ai",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@google/genai": "^1.28.0",
    "react-dom": "^19.2.0",
    "react": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
{
  "scripts": {
    "build": "next build",
    "start": "next start -p 3000",
    "e2e": "playwright test",
    "ci": "npm run build && (npm run start &) && npx wait-on http://localhost:3000 && npm run e2e"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.9.0",
    "@playwright/test": "^1.48.0",
    "pixelmatch": "^5.3.0",
    "pngjs": "^7.0.0",
    "wait-on": "^7.2.0"
  }
}

